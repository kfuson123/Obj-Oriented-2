#include "game.hpp"
void deal_one(Deck &d, Player& p);

void add(Player &player)
{
   int p = p + 1;
}

bool empty(const Deck& d)
{
    if(d.size() > 0)
        return false;
    else
        return true;
}
